;; A-side disequality with nested B-local functions: the B summary is a
;; Horn clause with two premises, and the lemma interpolant is its negation.
;;   expected: (not (=> (and (= x y) (= p q)) (= u v)))
(set-option :sat.euf true)
(set-option :tactic.default_tactic sat)
(set-option :solver.proof.log horn_dual_mixed_proof.smt2)
(set-option :solver.proof.interpolate_log true)
(declare-sort U)
(declare-const x U) (declare-const y U) (declare-const p U) (declare-const q U)
(declare-const u U) (declare-const v U)
(declare-fun F (U U) U) (declare-fun G (U) U)
(set-itp-group A)
(assert (= x y))
(assert (= p q))
(assert (not (= u v)))
(set-itp-group B)
(assert (= u (F (G x) p)))
(assert (= v (F (G y) q)))
(check-sat)
